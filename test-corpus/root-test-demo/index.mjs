// A minimal source module. The point of this fixture is the sibling test.mjs:
// a repo-root suite named exactly test.mjs — the konomi single-file convention.
export const add = (a, b) => a + b;
export const mul = (a, b) => a * b;
