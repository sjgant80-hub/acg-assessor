# Inventory

A tiny inventory list library. Add, remove, count, and find items.

## Run

```
npm test
```

Pure functions, no dependencies. This is a fixture in the assessor's regression suite: it is built
to **pass** the rubric, and the suite fails if it ever stops passing.
